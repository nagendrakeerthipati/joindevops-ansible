- hosts: local
  gather_facts: false

  vars:
    server_name: roboshop.com
    port: 80

    backend_servers:
      - 10.0.0.5
      - 10.0.0.6
      - 10.0.0.7

  tasks:
    - name: generate nginx config
      template:
        src: nginx.conf.j2
        dest: /tmp/nginx.conf