- name: loops 
  hosts: local
  become: true
  tasks:
    - name: installing package 
      ansible.builtin.package:
        name: "{{ item.name }}"
        state: "{{ item.state }}"
      loop:
      - {{name: nginx, state:}}