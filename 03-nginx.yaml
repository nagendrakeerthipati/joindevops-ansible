- name: install nginx
  hosts: frontend
  become: true
  tasks:
    - name: installing nginx
      ansible.builtin.package:
        name: nginx
        state: present

    - name: enabling nginx
      ansible.builtin.service:
        name: nginx
        state: started
